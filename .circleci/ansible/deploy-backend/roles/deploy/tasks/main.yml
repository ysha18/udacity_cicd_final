- name: "Copy backend files"
  ansible.builtin.copy:
    src: /tmp/dist/
    dest: $HOME

- name: Install Node Modules
  command: |
    npm i
    npm run build

- name: "run app using pm2"
  command: |
    set -ex
    pm2 start main.js